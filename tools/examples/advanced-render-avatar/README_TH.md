# Advanced Custom Render Example

ตัวอย่างนี้สาธิต `rect`, `outline`, `polyline`, render group, task handle, `render.update`, responsive HUD ด้วย `render.screen()` และ world-anchored task

คัดลอกทั้งโฟลเดอร์ไปที่ `shyne-mods/avatars/` แล้วอนุมัติ `hud_render` กับ `world_render` ตัวอย่างเป็น Creator fixture ภายนอกและไม่ถูกบรรจุใน Mod JAR
