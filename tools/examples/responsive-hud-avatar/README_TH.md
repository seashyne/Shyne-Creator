# Responsive HUD Example

ตัวอย่าง HUD ขนาดเล็กที่จัดกึ่งกลางตามความกว้างหน้าจอและ GUI scale โดยใช้ `render.screen()`, group, layer, rectangle และ outline

ต้องอนุมัติเฉพาะ permission `hud_render` และตัวอย่างนี้ไม่ถูกบรรจุใน Mod JAR
